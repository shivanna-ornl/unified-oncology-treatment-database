# Local raw-data directory

Raw source files are intentionally not distributed in this repository. Follow
`docs/data_sources.md`, place the files under the paths shown in
`config/sources.example.json`, and copy that file to `config/sources.json`.

Everything below this directory is ignored by Git except this README and the
placeholder file.

